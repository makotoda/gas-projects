# Konfigurasi lokal Claude Code (izin per mesin, bukan bagian dari project)
.claude/settings.local.json

# Kalau suatu saat pakai npm/node
node_modules/

# Output knowledge graph graphify (regenerate: graphify . --code-only)
graphify-out/

# Local price helper files
harga.gs.js
